<form method="post" action="{{ path('app_type_activate', {'id': type.id}) }}" onsubmit="">
    <input type="hidden" name="_token" value="{{ csrf_token('activate' ~ type.id) }}">
    <button class="btn btn-primary">Activer</button>
</form>
