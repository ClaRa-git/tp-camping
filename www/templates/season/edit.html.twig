{% extends 'base.html.twig' %}

{% block title %}Edition de la saison{% endblock %}

{% block body %}
    <h1>Modifier la saison</h1>

    <h2>{{ season.label }}</h2>

    {{ include('season/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_season_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {{ include('season/_delete_form.html.twig') }}
    </div>
{% endblock %}
