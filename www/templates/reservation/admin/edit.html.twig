{% extends 'base.html.twig' %}

{% block title %}Edition de la réservation{% endblock %}

{% block body %}
    <h1>Modifier la réservation</h1>

    {{ include('reservation/admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_reservation_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {{ include('reservation/admin/_delete_form.html.twig') }}
    </div>
{% endblock %}
