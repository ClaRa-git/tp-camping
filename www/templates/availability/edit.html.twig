{% extends 'base.html.twig' %}

{% block title %}Edition de la non disponibilté{% endblock %}

{% block body %}
    <h1>Modifier la non disponibilité</h1>

    {{ include('availability/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_availability_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {# {{ include('availability/_delete_form.html.twig') }} #}
    </div>
{% endblock %}
