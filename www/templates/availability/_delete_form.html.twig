<form method="post" action="{{ path('app_availability_delete', {'id': availability.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ availability.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
