{% extends 'base.html.twig' %}

{% block title %}Edition de l'équipement{% endblock %}

{% block body %}
    <h1>Modifier l'équipement</h1>

    <h2>{{ equipment.label }}</h2>

    {{ include('equipment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_equipment_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {{ include('equipment/_delete_form.html.twig') }}
    </div>
{% endblock %}
