{% extends "base.html.twig" %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <h1>Dashboard</h1>

    <div class="action-buttons">
        <a href="{{path('app_rental_index')}}" class="btn btn-edit">Voir les locations</a>
        <a href="{{path('app_type_index')}}" class="btn btn-edit">Voir les types d'hébergements</a>
        <a href="{{path('app_user_index')}}" class="btn btn-edit">Voir les utilisateurs</a>
        <a href="#" class="btn btn-edit">Voir les réservations</a>
    </div>
    <div class="action-buttons">
        <a href="{{path('app_season_index')}}" class="btn btn-edit">Voir les saisons</a>
        <a href="{{path('app_price_index')}}" class="btn btn-edit">Voir les prix par saison</a>
        <a href="{{path('app_equipment_index')}}" class="btn btn-edit">Voir les équipements</a>
        <a href="{{path('app_availability_index')}}" class="btn btn-edit">Voir les disponibilités</a>
    </div>
{% endblock %}
